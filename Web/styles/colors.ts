/**
 * @description An object with the default used colors on
 * the application for each use, example: 
 * ```
 * {
 *   danger: 'red.500'
 * }
 * ```
 */
const colors = {
    success: 'green.400',
    danger: 'red.400',
    warning: 'yellow.400'

};

export default colors;